version: '3'
services:
  web:
    image: niceguy420/app:latest
    restart: always
    build: .
    external_links:
      - mongo
    scale: 5

  mongo:
    image: mongo
    ports:
      - '27017:27017'
    volumes:
      - db-data:/foo

  loadbalancer:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - web
    ports:
      - "4000:4000"

volumes:
  db-data: